PUBLIC_SITE_TITLE="Eat Poop You Cat"
PUBLIC_BASE_URL=http://localhost:3792
BETA=true

# Optional: Social and donation links (omit to hide these links)
# PUBLIC_OWNER_NAME="John Doe" # For the footer copyright
# PUBLIC_OWNER_URL=http://example.com # For the footer copyright
# PUBLIC_DISCORD_INVITE_ID=AbCdEfGhIj # Link in footer, contact page
# PUBLIC_LIBERAPAY_ID=AbCdEfGhIj # Donation link on contact page

### Sentry Error Tracking (Optional - only needed for production error monitoring)
# PUBLIC_SENTRY_DSN=https://0123456789abcdef.ingest.us.sentry.io/0123456789abcdef
# SENTRY_ORG=your-org-slug  # Required for source map uploads - cannot be inferred from DSN
# SENTRY_PROJECT=your-project-slug  # Required for source map uploads - cannot be inferred from DSN
# SENTRY_AUTH_TOKEN=sntrys_xxx  # Required for uploading source maps during build

PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_abcdefghijklmnopqrstuvwxyz0123456789abcdef
CLERK_SECRET_KEY=sk_test_abcdefghijklmnopqrstuvwxyz0123456789abcdef

### These are supplied by Vercel in production
SUPABASE_SERVICE_ROLE_KEY=xxxx
POSTGRES_URL_NON_POOLING="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
PUBLIC_SUPABASE_URL="http://127.0.0.1:54321" # IMPORTANT: Set public prefix to "PUBLIC_" in Vercel

### This isn't but I wish it was
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres" # IMPORTANT:May require pgbouncer=true&connection_limit=1
DATABASE_URL_TEST="postgresql://postgres:postgres@127.0.0.1:5432/test" # For integration tests

REDIS_URL=redis://localhost:6379

PUBLIC_DISABLE_POLLING=false

ADMIN_EMAIL=admin@example.com
FROM_EMAIL=noreply@example.com

# Email Configuration
# For production, set:
# BREVO_API_KEY=your_brevo_api_key_here

# Inbucket/SMTP Development Email (used when BREVO_API_KEY is not set)
# These work with Supabase dev setup out of the box
INBUCKET_URL=http://127.0.0.1:54324
SMTP_HOST=127.0.0.1
SMTP_PORT=54325


